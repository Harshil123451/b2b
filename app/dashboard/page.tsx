/**
 * Dashboard Page
 * Uses client-side wrapper to handle authentication
 */

import DashboardWrapper from '@/components/DashboardWrapper';

export default function DashboardPage() {
  return <DashboardWrapper />;
}

